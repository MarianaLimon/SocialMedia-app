import React from "react";
import AppImage from "./AppImage";
import "./appAvatar.css";

export default function AppAvatar(props) {
  const { pathImage, classImage, altImage } = props;
  //profile, avatar,
  return;

  <AppImage
    pathImage={pathImage}
    classImage={classImage}
    altImage={altImage}
  />;
}
